/**
 * state
 */
const initState = {
  articlesList: [],
  total: 0
};

/**
 * reducer
 * @param {*} state
 * @param {*} action
 */

export function articles(state = initState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
